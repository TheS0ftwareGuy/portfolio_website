{
  "name": "portfolio_website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && rm -f package-lock.json && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
} 